<% if page.current? %>
  <span class="bg-blue-500 text-white px-3 py-1 rounded">
    <%= page %>
  </span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "bg-gray-700 hover:bg-gray-600 text-gray-300 px-3 py-1 rounded transition-colors duration-200" %>
<% end %>